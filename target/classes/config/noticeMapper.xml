<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="notice">
	<select id="noticelist" parameterType="map" resultType="dto.NoticeDTO">
		select b.*
		from(
			select rownum rm,a.*
			from(	select n.*, a.user_photo, a.user_nickname
					from moons_notice n, moons_user u, moons_user a
					where n.user_code=#{user_code} and n.notice_actor=a.user_code and n.user_code=u.user_code
					order by n.notice_date desc)a)b
		<![CDATA[where rm>#{start} and rm<=#{start}+10]]>
	</select>
	
	<update id="readcheck" parameterType="int">
		update moons_notice
		set notice_read=1
		where user_code=#{user_code} 
		<![CDATA[and notice_date<=sysdate]]>
	</update>
</mapper>